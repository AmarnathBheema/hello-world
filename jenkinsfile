pipeline

agent any
def mvnHome = tool name: my-mvn

stages{
 stage('git checkout'){
  steps{
  git 'https://github.com/AmarnathBheema/hello-world.git'
  }
 }
 stage('build'){
  steps{
  sh "${mvnHome}/bin clean package"
  }
 }
 }
